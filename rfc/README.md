# RFCs (short form)
Each change to GCS must be proposed via a short RFC:

- Context / Problem
- Proposal (API, schema, UX)
- Examples (req/resp)
- Migration / Back-compat
- Rollout plan

Name files as `0001-short-title.md`.
